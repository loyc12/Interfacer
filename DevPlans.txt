TO FIX :

No bugs for now



TO DO :

Finish implementing ./claude/TOKEN_EFFICIENCY_PLAN.md
Split the .gitignore and .vscodeignore filter options into two separate tickboxes
Add a limited sent message history we can "scroll" through with arrow keys, i ncase we want to send an old message ( keep in mind potential future complete message/response history )
Add a token cost estimation based on the model chosen and official Claude Api prices and consumption.
Add meaningful, permanent debug log messages, active only if a "devMode" variable is true, to more easily find behaviour issues when bug arise ( add "put useful debug logs" rule to CLAUDE.md )



TO CONSIDER :

Check if adding a message history could be worht the hassle ( simplicity vs usefulness ). History ought to be capped to a custom amount of token / messages or smthg
Look for meaningful polishing / QoL features to add ( add "auto suggest tweaks" guideline to CLAUDE.md ? )
Check if 150KB is large for an extension, and make sure to be resonably efficient memory-wise ( no need to waste compute if we could trivially not do so ) ( add "memory compactness" guideline to CLAUDE.md )
Add tools to semi-agentify context gathering, instead of giving giant context